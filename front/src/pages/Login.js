import React from 'react';
import styled from 'styled-components';

const LoginContainer = styled.section`
  width: 100%;
  height: 100%;
  display: flex;
  justify-content: center;
  align-items: center;
`;

const Login = () => {
  return <LoginContainer>나는 로그인페이지</LoginContainer>;
};

export default Login;
