const testCalendar = ['2021년 12월10일', '2021년 12월4일'];

const calendarReducer = (state = testCalendar, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default calendarReducer;
